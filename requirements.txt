# Dependencies are now declared canonically in pyproject.toml and pinned in uv.lock.
#
#   Runtime deps ............ [project.dependencies]
#   Dev/test/lint tools ..... [dependency-groups] dev
#   Real-inference stack .... [project.optional-dependencies] real
#
# Recommended (reproducible, uses uv.lock):
#   uv sync --locked --dev
#
# Plain pip alternative (resolves fresh, no lockfile):
#   pip install -e .            # runtime only
#   pip install -e '.[real]'    # + real-inference stack
